{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": ["...", "pnpm turbo build --filter=erp-web"]
    }
  },
  "deploy": {
    "startCommand": "pnpm --filter erp-web start"
  }
}
